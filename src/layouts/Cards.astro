---
import type { ImageMetadata } from 'astro';
import { Image } from 'astro:assets';

interface Props {
   imagePath: string;
   altText: string;
   name: string;
   age: string;
}

const { imagePath, altText, name , age} = Astro.props;

---


<div class="gid grid-cols-3">
    <!--img class="w-full aspect-video" src={imagePath} alt={altText} /-->
    <h2>{name}</h2>
</div>

